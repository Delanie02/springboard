const friend = "BRUTUS"
const shiftValue = 3;

// Step 1
const alphabet = "abcdefghijklmnopqrstuvwxyz";

// Step 2
const newIndex = index + shiftValue; // i + 3
const encryptedFirstLetter = alphabet[newIndex];
const encryptedMessage = "";
for (let i = 0; i < friend.length; i++) {

}  